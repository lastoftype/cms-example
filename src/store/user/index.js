import * as types from './action-types';

const INITIAL_STATE = {
	user: {},
	loading: false
};

const user = (state = INITIAL_STATE, action) => {
	switch(action.type) {
		default:
			return state;
	}
};

export default user;